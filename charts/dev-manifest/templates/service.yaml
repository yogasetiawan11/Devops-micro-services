apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-manifest.fullname" . }}
  labels:
    {{- include "dev-manifest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-manifest.selectorLabels" . | nindent 4 }}
