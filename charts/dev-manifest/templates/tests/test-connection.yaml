apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev-manifest.fullname" . }}-test-connection"
  labels:
    {{- include "dev-manifest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dev-manifest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
