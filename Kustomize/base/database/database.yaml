

---
### Structure config to Create An Ebs volume for the database in eks.
#apiVersion: storage.k8s.io/v1
#kind: StorageClass
#metadata:
#  name: standard
#  provisioner: rancher.io/local-path
#reclaimPolicy: Retain
#volumeBindingMode: WaitForFirstConsumer
#allowVolumeExpansion: true



---

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-postgres
  namespace: dev
  labels:
    app.kubernetes.io/part-of: dev
    app.kubernetes.io/component: database
    app.kubernetes.io/name: db-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: db-postgres
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: dev
        app.kubernetes.io/component: database
        app.kubernetes.io/name: db-postgres
      # Security: don't automount service account token (DB doesn't need K8s API)
    spec:
      automountServiceAccountToken: false
      tolerations:
        - key: "node.kubernetes.io/not-ready"
          operator: "Exists"
          effect: "NoExecute"
          tolerationSeconds: 300
      containers:
        - name: postgres 
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
              protocol: TCP
          envFrom:
            - secretRef: 
                name: db-credentials
          volumeMounts:
            - name: db-storage
              mountPath: /var/lib/postgresql/data
              subPath: data
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false  # Postgres needs write access
            runAsNonRoot: false  # Postgres image runs as root by default
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "yogasetiawan34234", "-d", "db-demo"]
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "yogasetiawan34234", "-d", "db-demo"]
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
      volumes:
        - name: db-storage
          persistentVolumeClaim:
            claimName: db-pvc


---

apiVersion: v1
kind: Service
metadata:
  name: database
spec:
  selector:
    app.kubernetes.io/name: db-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: dev
  labels:
    app.kubernetes.io/part-of: dev
    app.kubernetes.io/component: database
    app.kubernetes.io/name: db-postgres
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 8Gi


---


apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: standard
provisioner: rancher.io/local-path # indicates that this StorageClass does not support automatic provisioning
reclaimPolicy: Retain
volumeBindingMode: WaitForFirstConsumer
parameters:
  guaranteedReadWriteLatency: "true"

---

apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: dev
  labels:
    app.kubernetes.io/part-of: dev
type: Opaque
data:
  POSTGRES_USER: eW9nYXdzZXRpYXdhbjM0MjM0
  POSTGRES_PASSWORD: YWluZy1tYWNhbi1rdW1iYW5nMDkwMDk5MDk5OTA5
  POSTGRES_DB: ZGItZGVtbw==

---

apiVersion: v1
kind: Namespace
metadata:
  name: dev
  labels:
    app.kubernetes.io/part-of: dev
  